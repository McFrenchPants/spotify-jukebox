import { SearchAndQueue } from '../components/search/SearchAndQueue'

/** Find Music tab (P4.8) — the existing P4.2 search + add-to-queue UI, moved here unchanged. */
export function SearchPage() {
  return <SearchAndQueue />
}
